import React from "react";
import { Text } from "react-native";

const MyBookScreen = ({ navigation }) => {
  return (
    <Text flex={1}>My Books</Text>
  );
};

export default MyBookScreen;