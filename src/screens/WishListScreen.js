import React from "react";
import { Text } from "react-native";

const WishlistScreen = ({ navigation }) => {
  return (
    <Text flex={1}>Wishlist</Text>
  );
};

export default WishlistScreen;